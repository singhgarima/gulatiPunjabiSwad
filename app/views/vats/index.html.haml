- title "Vats"
%table
  %tr
    %th Tax
  - for vat in @vats
    %tr
      %td= vat.tax
      %td= link_to "Show", vat
      %td= link_to "Edit", edit_vat_path(vat)
      %td= link_to "Destroy", vat, :confirm => 'Are you sure?', :method => :delete
%p= link_to "New Vat", new_vat_path
